{
  "name": "ambassador-scheduler",
  "version": "1.0.0",
  "description": "Full-stack app for scheduling student ambassadors to events.",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w backend & npm run dev -w frontend",
    "backend": "npm run dev -w backend",
    "frontend": "npm run dev -w frontend"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  }
}
